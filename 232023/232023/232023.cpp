#include <iostream>
#include <ctime>
#include "Die.h"
#include "Player.h"
using namespace std;
void showDice(Die[]);
int getTotalScore(Die[]);
int main()
{
    srand(time(NULL));
    //Die bones[10];
    Player me;
    me.getTotal();
    int getTotalScore(Die theDice[]);
    void showDice(Die theDice[]);
}